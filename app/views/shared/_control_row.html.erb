<% if row_class(control) == 'incorporated-into' %>
<tr class="<%= row_class(control) %>">
  <td scope="row">
    <%= control[:label] %>
  </td>
  <td><%= control[:title] %></td>
  <td colspan="4">
    Incorporated into
    <%= control.links.where(link_type: 'incorporated-into').pluck(:link_text).to_sentence.gsub('#','').upcase %>.
  </td>
</tr>
<% elsif row_class(control) == 'moved-to' %>
<tr class="<%= row_class(control) %>">
  <td scope="row">
    <%= control[:label] %>
  </td>
  <td><%= control[:title] %></td>
  <td colspan="4">
    Moved to
    <%= control.links.where(link_type: 'moved-to').pluck(:link_text).to_sentence.gsub('#','').upcase %>.
  </td>
</tr>
<% else %>
<tr data-link="<%= control_path(control.label) %>" class="<%= row_class(control) %>">
  <td scope="row">
    <%= link_to control_path(control.label) do %>
    <%= control.label %>
    <% end %>
  </td>
  <td><%= control[:title] %></td>
  <td class="text-center">
    <% if control.is_low %>
      <i class="fa fa-check-circle text-success" aria-hidden="true"></i>
    <% else %>
      <i class="fa fa-times-circle text-muted" aria-hidden="true"></i>
    <% end %>
  </td>
  <td class="text-center">
    <% if control.is_moderate %>
      <i class="fa fa-check-circle text-success" aria-hidden="true"></i>
    <% else %>
      <i class="fa fa-times-circle text-muted" aria-hidden="true"></i>
    <% end %>
  </td>
  <td class="text-center">
    <% if control.is_high %>
      <i class="fa fa-check-circle text-success" aria-hidden="true"></i>
    <% else %>
      <i class="fa fa-times-circle text-muted" aria-hidden="true"></i>
    <% end %>
  </td>
  <td class="text-center">
    <% if control.is_privacy %>
      <i class="fa fa-check-circle text-success" aria-hidden="true"></i>
    <% else %>
      <i class="fa fa-times-circle text-muted" aria-hidden="true"></i>
    <% end %>
  </td>
</tr>
<% end %>