<% unless references.empty? %>
<ul>
  <% references.each do |r| %>
  <li>
    <%= link_to(r.href) do %>
    <strong><%= r.number %></strong> <i class="fas fa-external-link-alt"></i>
    <% end %> <em><%= r.citation %></em>
  </li>
  <% end %>
</ul>
<% end %>